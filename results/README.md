# Results from several training runs

The txt and csv files are log files of the training runs, containing the training and validation loss, and the validation metrics (optionally).
[loss-eval.json](loss-eval.json) is the results file for the evaluation of different loss functions on the training set.
[ensemble-eval.json](ensemble-eval.json) is the results file for the evaluation of ensemble models on the training set.
